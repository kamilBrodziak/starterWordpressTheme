<ul class="searchResultsList">
    {% if posts is empty %}
        <span class="searchResultsInfo flexCentered">Brak produktów o wyszukiwanej frazie</span>
    {% endif %}
    {% for post in posts %}
        <li class="searchResultsListItem">
            <a href="{{ post.link }}" class="searchResultAnchor flexCentered">
                {{ post.title }}
            </a>
        </li>
    {% endfor %}
</ul>